# Auto detect text files and perform LF normalization
* text=auto

# Set default behavior for all text files to use LF line endings.
* text=auto

# Force line endings to LF for shell scripts.
*.sh text eol=lf

# Force line endings to CRLF for Windows batch files.
*.bat text eol=crlf

# Treat image files as binary, so they won’t be diffed.
*.jpg binary
*.png binary

# Use a custom diff tool for markdown files.
*.md diff=markdown

# Don't show diffs for log files.
*.log -diff
